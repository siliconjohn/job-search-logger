noopener noreferrer for security on links

when to use eseImparativeHandle

Use it when a parent comp needs explicit 
control of a child's methods. For example
a child component has a play button which needs focus
The parent may need to call focus() on a specifice Child


contct
import createContect


declare context
const ThemeContext = ucreateContext( null )

set the value of it when instantiate component

<ThemeContext value="dark">
    <Form/>
<ThemeContect/>


const Form = () => {
    const Context = useContext( Thene)


}